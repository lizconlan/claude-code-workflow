# Sprint 2: CSS Styling

## Goal
Style the application using the design tokens defined in SPEC.md.

## Tasks

- [x] Define CSS custom properties (design tokens) on `:root`
- [x] Style `body`: background, font, centred layout
- [x] Style `h1` header
- [x] Style `#scoreboard`: flex row, evenly spaced score items
- [x] Style `#status`: clear, readable turn/result message
- [x] Style `#board`: 3×3 CSS Grid
- [x] Style `.cell` buttons: size, border, font, cursor, hover, focus-visible
- [x] Style `.win` class: winning cell highlight
- [x] Style `#reset-btn`: filled primary colour, hover state
- [x] Ensure colour contrast meets WCAG AA (4.5:1)
