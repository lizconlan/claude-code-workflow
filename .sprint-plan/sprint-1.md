# Sprint 1: Project Scaffold + HTML Structure

## Goal
Create the three-file project structure and write the base HTML markup.

## Tasks

- [x] Create `index.html` with DOCTYPE, head (meta charset, viewport, title, CSS link)
- [x] Add `<header>` with `<h1>Tic-Tac-Toe</h1>`
- [x] Add `<main>` containing:
  - [x] `#scoreboard` section with three score divs (X, Draw, O)
  - [x] `#status` paragraph with `aria-live="polite"`
  - [x] `#board` section (`role="grid"`) — cells will be injected by JS
  - [x] `#reset-btn` button
- [x] Create empty `style.css` (linked from index.html)
- [x] Create empty `app.js` (linked from index.html)
