# Sprint 3: Core Game Logic

## Goal
Implement pure game logic functions (no DOM) that can be unit-tested.

## Tasks

- [x] Define `WIN_COMBOS` constant (8 winning lines)
- [x] Implement `checkWinner(board)` → returns `{ winner, combo }` or `null`
- [x] Implement `checkDraw(board)` → returns `true` if full board with no winner
- [x] Write `test.js` with tests T-01 through T-07 (pure logic tests)
- [x] Run tests; all pass
