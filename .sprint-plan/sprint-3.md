# Sprint 3: Core Game Logic

## Goal
Implement pure game logic functions (no DOM) that can be unit-tested.

## Tasks

- [ ] Define `WIN_COMBOS` constant (8 winning lines)
- [ ] Implement `checkWinner(board)` → returns `{ winner, combo }` or `null`
- [ ] Implement `checkDraw(board)` → returns `true` if full board with no winner
- [ ] Write `test.js` with tests T-01 through T-07 (pure logic tests)
- [ ] Run tests; all pass
